# Database Configuration
DATABASE_URL="file:/path/to/your/project/prisma/dev.db"

# NextAuth Configuration
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3004"
NEXTAUTH_SECRET="your-secret-key-change-this-in-production"

# Email Configuration (SMTP)
# For Gmail:
# 1. Enable 2-Step Verification
# 2. Generate an App Password: https://support.google.com/accounts/answer/185833
# 3. Use the App Password as SMTP_PASSWORD

# For other email providers (SendGrid, Mailgun, etc.):
# Use their SMTP settings

SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-app-password-or-smtp-password"
SMTP_FROM="your-email@gmail.com"

# Production Settings
# When deploying to production:
# 1. Change NEXTAUTH_URL to your production URL
# 2. Generate a new NEXTAUTH_SECRET
# 3. Use production email credentials
# 4. Consider using PostgreSQL instead of SQLite for DATABASE_URL
